class SearchTool:
    """
    Placeholder "search" tool.
    Replace with a real search backend (web, internal index, DB, etc.).
    """
    name = "search"

    def run(self, query: str) -> str:
        # Intentionally not performing real web requests in this template.
        return f"[search-placeholder] query={query}"
