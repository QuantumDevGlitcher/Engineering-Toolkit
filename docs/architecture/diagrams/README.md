# Diagrams

Default format: **Mermaid** (`.mmd`) so diagrams are versionable as text.

Optional:
- Use Draw.io/Excalidraw for complex diagrams.
- Keep the source file and export PNG/SVG when needed.

## Rules
- Every diagram should have a clear title and purpose.
- Prefer minimal diagrams first; expand when complexity grows.
