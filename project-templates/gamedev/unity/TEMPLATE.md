# GameDev Template — Unity

## Recommended docs profile
- `game` (enables `docs/design/`)

## Notes
- Use Git LFS for large binary assets (textures/audio/models).
- Keep build/export scripts in `scripts/`.
- Track major system decisions with ADRs (AI, worldgen, networking).
