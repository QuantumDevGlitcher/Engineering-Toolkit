# GameDev Template — Unreal

## Recommended docs profile
- `game` (enables `docs/design/`)

## Notes
- Use Git LFS for large assets.
- Keep build/package scripts in `scripts/`.
- Capture major architectural changes with ADRs.
